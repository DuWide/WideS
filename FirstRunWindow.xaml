<Window x:Class="DevCockpit.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="WideS"
        Icon="Assets/WideS.ico"
        Width="520"
        Height="390"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBgBrush}"
        KeyDown="Window_KeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="32" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="True" />
    </shell:WindowChrome.WindowChrome>
    <Grid Margin="26" Background="{StaticResource AppBgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Первый запуск WideS" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
            <TextBlock Text="Задайте имя для приветствия и пароль входа. Пароль можно оставить пустым." Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,8,0,20" />
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,8,0,8">
            <StackPanel>
                <TextBlock Text="Имя пользователя" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6" />
                <TextBox x:Name="UserNameBox" Margin="0,0,0,14" />
                <TextBlock Text="Пароль" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6" />
                <PasswordBox x:Name="PasswordBox" Padding="10" Margin="0,0,0,14" />
                <TextBlock Text="Подтвердите пароль" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6" />
                <PasswordBox x:Name="ConfirmPasswordBox" Padding="10" />
                <TextBlock x:Name="ErrorText" Foreground="{StaticResource DangerBrush}" Margin="0,10,0,0" />
            </StackPanel>
        </Border>
        <WrapPanel Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="Продолжить" Style="{StaticResource PrimaryButton}" Click="Continue_Click" />
        </WrapPanel>
    </Grid>
</Window>
