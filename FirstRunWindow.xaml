<Window x:Class="DevCockpit.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WideS"
        Icon="Assets/WideS.ico"
        Width="560"
        Height="470"
        MinWidth="520"
        MinHeight="440"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Background="Transparent"
        KeyDown="Window_KeyDown">
    <Border Style="{StaticResource DialogFrame}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="*" />
                <RowDefinition Height="58" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource DialogHeader}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel>
                    <Button DockPanel.Dock="Right" Style="{StaticResource DialogCloseButton}" Click="Close_Click">
                        <Path Data="{StaticResource CloseIconGeometry}" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="13" Height="13" Stretch="Uniform" />
                    </Button>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <Image Source="Assets/WideS.png" Width="20" Height="20" Margin="0,0,8,0" />
                        <TextBlock Text="Первый запуск" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="28,22,28,16">
                <StackPanel>
                    <TextBlock Text="Настроим WideS" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="Имя появится на экране приветствия. Пароль можно оставить пустым."
                               Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,6,0,22" />

                    <TextBlock Text="Имя пользователя" Style="{StaticResource FieldLabelText}" />
                    <TextBox x:Name="UserNameBox" Margin="0,0,0,16" />
                    <TextBlock Text="Пароль" Style="{StaticResource FieldLabelText}" />
                    <PasswordBox x:Name="PasswordBox" Margin="0,0,0,16" />
                    <TextBlock Text="Подтвердите пароль" Style="{StaticResource FieldLabelText}" />
                    <PasswordBox x:Name="ConfirmPasswordBox" />
                    <TextBlock x:Name="ErrorText" Foreground="{DynamicResource DangerBrush}" Margin="0,10,0,0" />
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Style="{StaticResource DialogFooter}">
                <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Button Content="Продолжить" Style="{StaticResource PrimaryButton}" Click="Continue_Click" />
                </WrapPanel>
            </Border>
        </Grid>
    </Border>
</Window>
