# WinForms UI (архив)

Не компилируется. Заменено WPF-окнами (`*EditorWindow.xaml`, `MainWindow`).

`ProjectDialog.StyleTextBox` / `DarkButton` перенесены в `WinFormsDialogHelpers.cs` (корень проекта) для `ContextBuilderDialog.cs`.
